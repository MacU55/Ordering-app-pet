spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/db_shop?createDatabaseIfNotExist=true"
    username: root
    password: root
    hikari:
      maximum-pool-size: 10
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: false

logging:
  level:
    root: error

company:
  utility:
    enabled: true
  discount:
    enabled: true
  notification:
    email:
      enabled: true
    console:
      enabled: false
